const person = {
    firstName: 'John',
    lastName: 'Doe',
    age: 50,
    eyeColor: 'blue',
    sex: 'male',
};

console.log(person);
console.log('My name is', `${person.firstName} ${person.lastName}`);
console.log('Age is:', person.age);
